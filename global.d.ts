interface SpeechRecognition extends EventTarget {
    // Add methods/properties as needed (e.g., start(), stop(), etc.)
  }
  declare var SpeechRecognition: {
    prototype: SpeechRecognition;
    new(): SpeechRecognition;
  };